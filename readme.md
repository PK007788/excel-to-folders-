# Retinal Dataset Organizer

A desktop Python application for organizing retinal image datasets for machine learning binary classification.

The tool reads labels from an Excel workbook and copies `.tif` image files into:

```text
Output/
    Yes/
    No/